global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'mock-monitored-component'
    static_configs:
      - targets: ['${MOCK_MONITORED_COMPONENT_TARGET}']
        labels:
          instance: 'mock-component'

