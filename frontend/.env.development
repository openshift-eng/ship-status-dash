VITE_PUBLIC_DOMAIN=http://localhost:8180
VITE_PROTECTED_DOMAIN=http://localhost:8443
# Optional: override live-data poll interval in ms (default 300000 = 5 minutes).
# Prefer .env.development.local for short intervals during active testing (gitignored).
# VITE_DASHBOARD_REFRESH_INTERVAL_MS=15000
